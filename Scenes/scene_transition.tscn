[gd_scene format=3 uid="uid://doy2gu0dakdok"]

[ext_resource type="Texture2D" uid="uid://cvl0os8g7qldp" path="res://Assets/Sprites/Blank.png" id="2_frdye"]
[ext_resource type="Script" uid="uid://b5rs72b7rusel" path="res://Scripts/scene_transition.gd" id="3_8sxkv"]

[sub_resource type="Shader" id="Shader_vin40"]
code = "shader_type canvas_item;

uniform float spacing = 150;
uniform float animation_progress : hint_range(0.0, 1.0) = 0.0;
uniform float dot_size : hint_range(0.0, 1.0) = 1.0;
uniform vec4 dot_color : source_color = vec4(0.0, 0.0, 0.0, 1.0);

void fragment() {
    if (animation_progress <= 0.0) {
    }
	
    vec2 screen_size = vec2(1.0) / SCREEN_PIXEL_SIZE;
    vec2 grid_count = floor(screen_size / spacing); 
	
    vec2 norm_pos = UV * screen_size / (grid_count * spacing);
	
    float delay = (norm_pos.x + norm_pos.y) * 0.5;
	
    float visible_threshold = delay * 0.1 + 0.01;
    if (animation_progress < visible_threshold) {
        discard;
    } 
	
    float transition = 0.3;
    float scale = smoothstep(
        delay - transition,
        delay + transition,
        animation_progress * (1.0 + transition)
    ); 
	
    if (scale < 0.005) {
        discard;
    }
    
    vec2 grid_center = (floor(UV * screen_size / spacing) + 0.5) * spacing;
    float dist = length(UV * screen_size - grid_center);
    float dot_radius = dot_size * scale * spacing * 0.8;
    
    float alpha = 1.0 - smoothstep(
        max(dot_radius - 1.5, 0.0),
        dot_radius + 1.5, 
        dist
    );
    
    if (alpha < 0.01) discard; 
    
    COLOR = vec4(dot_color.rgb, alpha);
}"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_n7iwu"]
shader = SubResource("Shader_vin40")
shader_parameter/spacing = 125.0
shader_parameter/animation_progress = 0.50000002375
shader_parameter/dot_size = 1.0
shader_parameter/dot_color = Color(0.00041451462, 0.00071270054, 0.0013290645, 1)

[node name="scene_transition" type="Node2D" unique_id=1221159815]
process_mode = 3
z_index = 1000

[node name="CanvasLayer" type="CanvasLayer" parent="." unique_id=654516200]
layer = 9999

[node name="Sprite2D" type="Sprite2D" parent="CanvasLayer" unique_id=1172155389]
material = SubResource("ShaderMaterial_n7iwu")
position = Vector2(960, 540)
scale = Vector2(60, 33.750004)
texture = ExtResource("2_frdye")
script = ExtResource("3_8sxkv")
